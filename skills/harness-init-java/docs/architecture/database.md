# 数据库设计与SQL规范

## 数据表清单

{如果单数据源:}
| 表名 | Entity 类 | 说明 |
|------|-----------|------|
| `{table_name}` | `{entity_full_class_name}` | {表的业务含义} |

{如果多数据源，按数据源分组:}

### 数据源：{datasource_name}（`@DS("{ds_name}")`）

| 表名           | Entity 类                  | 说明           |
| -------------- | -------------------------- | -------------- |
| `{table_name}` | `{entity_full_class_name}` | {表的业务含义} |

#### {table_name} 字段说明

| 字段名         | Java 类型     | 数据库类型  | 说明                       |
| -------------- | ------------- | ----------- | -------------------------- |
| `{field_name}` | `{java_type}` | `{db_type}` | {字段含义，优先从注释推断} |

{注意：敏感字段（手机号、身份证等）标记为加密字段，后缀 \_encrypt}

---

## 命名规范

- 表名/字段名：`snake_case`（小写下划线）
- 主键：`bigint(20)` 自增
- 规范: 主键必须为bigint(20) 自增类型, 每个表必须要有`created_stime`(默认为当前时间), `modified_stime`(自动更新为当前时间), `is_del` (逻辑删除)。

## 敏感字段加密

以下字段必须加密存储，后缀 `_encrypt`：

- 密码、用户真实姓名、身份证号、银行卡号、个人住址、员工姓名、公司税务标识、属于 隐私信息，必须设计为加密存储 字段后缀为 _encrypt
- 手机号除了需要增加 `_encrypt`还需要增加后缀 `_hash`： `mobile`→ `mobile_encrypt` + `mobile_hash`

## SQL 安全红线

- **禁止** 无 WHERE 的 UPDATE/DELETE
- **禁止** 跨库联表查询
- **禁止** SELECT \*
- **要求** 批量操作加 LIMIT
- **要求** 大表查询走索引

## 检查工具

运行 `/sql-risk-review` 进行自动化 SQL 风险检查。

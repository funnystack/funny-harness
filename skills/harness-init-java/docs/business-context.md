## 业务上下文

> 以下内容由 harness-init-java 从代码中提取，请 Owner 逐条确认和补充。

## 核心业务名词

| 名词   | 对应类    | 说明                 | 确认 |
| ------ | --------- | -------------------- | ---- |
| {name} | `{class}` | {从注释或上下文推断} | [ ]  |

## 业务状态流转

{从 Enum 和 Service 方法推断出的状态流转，用文字描述}

| 业务对象 | 状态枚举       | 流转路径                         |
| -------- | -------------- | -------------------------------- |
| {entity} | `{enum_class}` | {state1} -> {state2} -> {state3} |

## 核心业务动作

| 动作     | Service 方法     | 涉及实体   | 确认 |
| -------- | ---------------- | ---------- | ---- |
| {action} | `{class.method}` | {entities} | [ ]  |

## 业务规则（从代码推断）

{从校验逻辑、条件判断中推断的业务规则}

| #   | 规则       | 来源位置        | 确认 |
| --- | ---------- | --------------- | ---- |
| 1   | {规则描述} | `{file}:{line}` | [ ]  |
